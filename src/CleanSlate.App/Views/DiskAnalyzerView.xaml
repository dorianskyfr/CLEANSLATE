<UserControl x:Class="CleanSlate.App.Views.DiskAnalyzerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Analyseur d'espace disque" Style="{StaticResource HeaderText}" />

        <!-- Barre d'action : lecteur + chemin + analyser -->
        <Grid Grid.Row="1" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ComboBox Grid.Column="0" Width="90" VerticalContentAlignment="Center"
                      ItemsSource="{Binding Drives}" SelectedItem="{Binding SelectedPath, Mode=TwoWay}" />

            <TextBox Grid.Column="1" Margin="8,0,8,0" Padding="8,6" VerticalContentAlignment="Center"
                     Text="{Binding SelectedPath, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Lecteur ou dossier à analyser (modifiable)." />

            <Button Grid.Column="2" Content="🔎 Analyser" Style="{StaticResource AccentButton}"
                    Command="{Binding AnalyzeCommand}" />
        </Grid>

        <!-- Note honnête -->
        <Border Grid.Row="2" Background="{DynamicResource SurfaceBrush}" CornerRadius="8"
                Padding="16" Margin="0,16,0,0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="ℹ️" FontSize="16" Margin="0,0,10,0" VerticalAlignment="Top" />
                <TextBlock Text="{Binding HonestNotice}" Style="{StaticResource MutedText}" MaxWidth="760" />
            </StackPanel>
        </Border>

        <!-- Statut -->
        <TextBlock Grid.Row="3" Text="{Binding Status}" Style="{StaticResource MutedText}"
                   Margin="2,12,0,0" TextWrapping="Wrap" />

        <!-- Résultats -->
        <ScrollViewer Grid.Row="4" VerticalScrollBarVisibility="Auto" Margin="0,12,0,0"
                      Visibility="{Binding HasResults, Converter={StaticResource BoolToVisibility}}">
            <ItemsControl ItemsSource="{Binding Results}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="6"
                                Padding="10,8" Margin="0,0,0,6">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="{Binding Icon}" FontSize="16"
                                           Margin="0,0,10,0" VerticalAlignment="Center" />

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" Foreground="{DynamicResource TextBrush}"
                                               FontSize="13" TextTrimming="CharacterEllipsis" />
                                    <ProgressBar Height="6" Minimum="0" Maximum="100" Margin="0,6,0,0"
                                                 Value="{Binding BarPercent, Mode=OneWay}"
                                                 Background="{DynamicResource BackgroundBrush}"
                                                 Foreground="{DynamicResource AccentBrush}" BorderThickness="0" />
                                </StackPanel>

                                <TextBlock Grid.Column="2" Text="{Binding SizeDisplay}" Margin="14,0,10,0"
                                           VerticalAlignment="Center" FontWeight="Bold"
                                           Foreground="{DynamicResource TextBrush}" />

                                <Button Grid.Column="3" Content="Ouvrir ↗" Style="{StaticResource SecondaryButton}"
                                        FontSize="11" Padding="8,4" VerticalAlignment="Center"
                                        Command="{Binding DataContext.OpenCommand,
                                            RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                        CommandParameter="{Binding}" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
