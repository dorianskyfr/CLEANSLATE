<UserControl x:Class="CleanSlate.App.Views.CleaningView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />   <!-- En-tête -->
            <RowDefinition Height="Auto" />   <!-- Lecteurs disponibles -->
            <RowDefinition Height="*" />      <!-- Liste des catégories -->
            <RowDefinition Height="Auto" />   <!-- Barre d'actions + statut -->
        </Grid.RowDefinitions>

        <!-- En-tête -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Nettoyage de fichiers temporaires" Style="{StaticResource HeaderText}" />
            <TextBlock Style="{StaticResource MutedText}" Margin="0,4,0,0"
                       Text="Analysez d'abord pour obtenir un aperçu. Rien n'est supprimé tant que vous n'avez pas confirmé." />
        </StackPanel>

        <!-- Lecteurs disponibles -->
        <ItemsControl Grid.Row="1" ItemsSource="{Binding AvailableDrives}" Margin="0,0,0,12">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="8"
                            Padding="14,10" Margin="0,0,10,0" MinWidth="150">
                        <StackPanel>
                            <TextBlock Text="{Binding DisplayName}"
                                       Foreground="{DynamicResource TextBrush}"
                                       FontWeight="SemiBold" FontSize="13" />
                            <ProgressBar Height="4" Margin="0,6,0,4" Minimum="0" Maximum="100"
                                         Value="{Binding UsedPercent, Mode=OneWay}"
                                         Background="{DynamicResource BackgroundBrush}"
                                         Foreground="{DynamicResource AccentBrush}" BorderThickness="0" />
                            <TextBlock Foreground="{DynamicResource MutedBrush}" FontSize="11">
                                <Run Text="{Binding FreeDisplay, Mode=OneWay}" />
                                <Run Text=" libre / " />
                                <Run Text="{Binding TotalDisplay, Mode=OneWay}" />
                            </TextBlock>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Liste des catégories -->
        <Border Grid.Row="2" Background="{DynamicResource SurfaceBrush}" CornerRadius="8" Padding="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Categories}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="12" Margin="4" CornerRadius="6"
                                    Background="{DynamicResource BackgroundBrush}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}"
                                              VerticalAlignment="Top" Margin="0,2,12,0" />

                                    <StackPanel Grid.Column="1">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding DisplayName}"
                                                       Foreground="{DynamicResource TextBrush}"
                                                       FontWeight="SemiBold" FontSize="14" />
                                            <Border Background="{DynamicResource SurfaceBrush}"
                                                    CornerRadius="4" Padding="6,1" Margin="8,0,0,0"
                                                    Visibility="{Binding RequiresAdministrator,
                                                        Converter={StaticResource BoolToVisibility}}">
                                                <TextBlock Text="admin requis" FontSize="10"
                                                           Foreground="{DynamicResource WarningBrush}" />
                                            </Border>
                                        </StackPanel>
                                        <TextBlock Text="{Binding Description}"
                                                   Style="{StaticResource MutedText}" Margin="0,4,0,0" />
                                    </StackPanel>

                                    <TextBlock Grid.Column="2" Text="{Binding RecoverableDisplay}"
                                               Foreground="{DynamicResource AccentBrush}"
                                               FontWeight="Bold" FontSize="14"
                                               VerticalAlignment="Center" Margin="12,0,4,0" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Barre d'actions + statut -->
        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Total + barre de progression + % -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" VerticalAlignment="Center">
                    <Run Text="Total récupérable :" Foreground="{DynamicResource MutedBrush}" />
                    <Run Text="{Binding TotalRecoverableDisplay, Mode=OneWay}"
                         Foreground="{DynamicResource AccentBrush}" FontWeight="Bold" />
                </TextBlock>
                <ProgressBar Grid.Column="1" Height="6" Margin="16,0,8,0"
                             Minimum="0" Maximum="100"
                             Value="{Binding ProgressValue, Mode=OneWay}"
                             Background="{DynamicResource SurfaceBrush}"
                             Foreground="{DynamicResource AccentBrush}" BorderThickness="0" />
                <TextBlock Grid.Column="2" Text="{Binding ProgressPercent, Mode=OneWay}"
                           Foreground="{DynamicResource AccentBrush}" FontWeight="Bold"
                           FontSize="12" VerticalAlignment="Center" MinWidth="36" />
            </Grid>

            <!-- Message de statut -->
            <TextBlock Grid.Row="1" Text="{Binding StatusMessage}"
                       Style="{StaticResource MutedText}" Margin="0,10,0,10" />

            <!-- Boutons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Annuler" Style="{StaticResource SecondaryButton}"
                        Command="{Binding CancelCommand}" />
                <Button Content="Analyser" Style="{StaticResource SecondaryButton}"
                        Command="{Binding ScanCommand}" />
                <Button Content="Nettoyer" Style="{StaticResource AccentButton}"
                        Command="{Binding CleanCommand}" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
